{
    "name": "doing-some-stuff",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ olofens/doing-some-stuff",
        "create-local": "graph create --node http://localhost:8020/ olofens/doing-some-stuff",
        "remove-local": "graph remove --node http://localhost:8020/ olofens/doing-some-stuff",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 olofens/doing-some-stuff"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.25.1",
        "@graphprotocol/graph-ts": "0.24.1"
    },
    "prettier": {
        "printWidth": 100,
        "tabWidth": 2
    }
}